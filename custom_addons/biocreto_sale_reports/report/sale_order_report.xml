<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Sobreescribir el reporte nativo para que apunte a tu template -->
    <record id="sale.action_report_saleorder" model="ir.actions.report">
        <field name="report_name">biocreto_sale_reports.report_quotation_biocreto</field>
        <field name="report_file">biocreto_sale_reports.report_quotation_biocreto</field>
        <field name="print_report_name">'Cotizacion - %s' % (object.name)</field>
        <field name="paperformat_id" ref="paperformat_biocreto_a4"/>
    </record>

    <!-- Sobreescribir también el reporte de Pro Forma Invoice si lo usan -->
    <!-- (opcional, descomentar si aplica) -->
    <!--
    <record id="sale.action_report_pro_forma_invoice" model="ir.actions.report">
        <field name="report_name">biocreto_sale_reports.report_quotation_biocreto</field>
        <field name="report_file">biocreto_sale_reports.report_quotation_biocreto</field>
    </record>
    -->

</odoo>
